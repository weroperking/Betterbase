# @betterbase/core

Core engine scaffold for BetterBase.

## Included

- `src/index.ts` with a typed `createCoreProject` helper
- `package.json` workspace wiring
- `tsconfig.json` strict TypeScript setup
