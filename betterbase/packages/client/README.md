# @betterbase/client

Client SDK scaffold for BetterBase.

## Included

- `src/index.ts` with a minimal `BetterBaseClient` class
- `package.json` workspace wiring
- `tsconfig.json` strict TypeScript setup
