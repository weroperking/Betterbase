// import { cron } from "@betterbase/core/iac";
// import { api } from "./_generated/api";
//
// Example: every day at midnight UTC, mark expired subscriptions as past_due.
// cron("subscription-reconcile", "0 0 * * *", api.mutations.tenants.reconcileExpired, {});
